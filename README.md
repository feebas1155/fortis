# Fortis Digital

Landing page and public site assets for Fortis Digital (fortisdigital.dev).

**Isolation rule:** this repo is standalone — do NOT mix with cairoyield / cheoma / vantex / gachapool projects. Private repo; license signing keys (`private.pem`) and buyer records (`licenses.log`) must NEVER be committed here.

- `index.html` — landing page (static, no build step)
- License system lives on VPS at `/root/feebas/license-system/` (not in this repo)

Deploy: static host (Netlify / Cloudflare Pages) auto-deploy from `main`.
